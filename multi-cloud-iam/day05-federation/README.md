# Day 5 – Federation: Azure → AWS (SSO) & JIT
Follow console steps to set SAML in Azure and hook to AWS Identity Center.
1) Set Identifier/ACS in Azure from AWS Issuer.
2) Upload Azure Federation Metadata XML into AWS.
3) Assign user to the Enterprise App, then test SSO.
