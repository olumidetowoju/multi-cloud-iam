# Day 8 – AWS EC2 Secure Deployments (Network + IAM + ECR)
## Run
```bash
cd day08-aws-ec2-secure/terraform
cp ../../terraform/aws/providers.tf ./providers.tf
terraform init && terraform apply -auto-approve
```
